import 'package:flutter/material.dart';

class RegionDetailsScreen extends StatelessWidget {
  const RegionDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
