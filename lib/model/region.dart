class Region {
  final String id;
  final String name;
  final double surface;  // You can use double for surface area (e.g., in square meters).

  Region({
    required this.id,
    required this.name,
    required this.surface,
  });
  }